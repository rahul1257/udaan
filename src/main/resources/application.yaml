spring:
  datasource:
    url: jdbc:mysql://localhost:3306/udaanspring
    username: udaanuser
    password: udaanpassword
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate.format_sql: true
server:
  port: 4567